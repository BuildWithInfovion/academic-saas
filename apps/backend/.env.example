# ─────────────────────────────────────────────────────────────────
# Academic SaaS — Backend environment variables
# Copy this file to .env and fill in your values.
# NEVER commit .env to version control.
# ─────────────────────────────────────────────────────────────────

# PostgreSQL connection string (Neon, Supabase, or local Postgres)
DATABASE_URL="postgresql://USER:PASSWORD@HOST/DATABASE?sslmode=require"

# JWT signing secret — generate with:
#   node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
# Must be at least 64 characters.
JWT_SECRET=REPLACE_WITH_STRONG_RANDOM_SECRET

# Port the backend listens on
PORT=3000

# Comma-separated extra allowed origins for CORS (optional)
# localhost:3000, localhost:3001, and https://*.vercel.app are always allowed.
# Add your custom production domain here:
# CORS_ORIGIN=https://app.yourdomain.com
